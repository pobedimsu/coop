{% extends 'user/_layout.html.twig' %}

{% block profile_content %}
    {% if form is defined %}
        <p>
            {{ app.user.username }}

            {% include 'user/_anketa.html.twig' with {'user': app.user} %}
        </p>

        {{ form(form) }}
    {% endif %}
{% endblock profile_content %}
