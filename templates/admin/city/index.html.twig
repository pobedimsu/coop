{% extends 'base.html.twig' %}

{% block title %}Admin - города{% endblock %}

{% block content %}

    <h1>Города</h1>

    <div class="row">
        <div class="col-md-6">
            <table class="table table-striped">
                <thead>
                <tr>
                    <th>id</th>
                    <th>Название</th>
                    <th>Дата создания</th>
                </tr>
                </thead>
                <tbody>
                {% for city in cities %}
                    <tr>
                        <td>
                            <a href="{{ path('admin_city_edit', {'id': city.id}) }}" title="Редактировать">
                                {{ city.id }}
                            </a>
                        </td>
                        <td>
                            <a href="{{ path('admin_city_edit', {'id': city.id}) }}" title="Редактировать">
                                {{ city }}
                            </a>
                        </td>
                        <td>{{ city.createdAt|date('Y-m-d H:i:s') }}</td>
                    </tr>
                {% endfor %}
                </tbody>
            </table>
        </div>

        <div class="col-md-6">
            <h3>Создать город</h3>

            {{ form(form) }}
        </div>
    </div>

{% endblock %}
