{% extends 'base.html.twig' %}

{% block title %}Заявка на предложение{% endblock %}

{% block content %}
    <h1>Заявка на предложение</h1>

    <div class="alert alert-danger" role="alert">
        Раздел в разработке, можно только добавить объявление, пока больше ничего сделать нельзя.
    </div>

    <p>{{ demand.createdAt|app_date }}</p>

    <h2>{{ demand.title }}</h2>

    <p>@todo картинку</p>

    <p>{{ demand.description }}</p>

    <p>Автор: <a href="#">{{ demand.user }}</a></p>

    <hr>
    <a href="{{ path('demand') }}">Назад</a> @todo с учетом страницы

    {% if is_granted('ROLE_ADMIN') %}
        <p>
            <a href="?remove" class="btn btn-danger">Удалить</a>
        </p>
    {% endif %}
{% endblock %}
